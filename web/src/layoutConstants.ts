/**
 * Canonical horizontal width for main content and nav (matches Tailwind `max-w-7xl`).
 * Use this so marketing, docs, dashboard, charts, and backtest align when navigating.
 */
export const SITE_CONTENT_MAX_WIDTH_PX = 1280;
